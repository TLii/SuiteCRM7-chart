apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "suitecrm7.fullname" . }}-test-connection"
  labels:
    {{- include "suitecrm7.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "suitecrm7.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
